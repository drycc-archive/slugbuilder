#!/usr/bin/env bash

SLUG_PATH=/tmp/slug.tgz
PROC_PATH=/tmp/build/Procfile

# shellcheck disable=SC1091
source /bin/normalize_storage
mc cp "$SLUG_PATH" minio/"${BUCKET}${PUT_PATH}/slug.tgz"
mc cp "$PROC_PATH" minio/"${BUCKET}${PUT_PATH}/Procfile"